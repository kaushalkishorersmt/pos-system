<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @item.name %>
</p>

<p>
  <strong>Price:</strong>
  <%= @item.price %>
</p>

<p>
  <strong>Tags:</strong>
  <% @tags.each do |tag| %>
    <%= "##{tag},"%>
  <% end %>
</p>

@item

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
